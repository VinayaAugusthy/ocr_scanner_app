abstract final class AppOpacity {
  AppOpacity._();

  /// Scan preview border when showing an error hint.
  static const double scanFrameErrorBorder = 0.6;

  /// Scan preview panel background tint.
  static const double scanFrameBackground = 0.35;
}
